{
  "name": "t3n-confidential-invoice-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A privacy preserving invoice approval agent built with T3N ADK.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs",
    "lint": "node --check src/decision.mjs && node --check src/agent.mjs && node --check web/server.mjs",
    "dev": "node web/server.mjs"
  },
  "dependencies": {
    "@terminal3/t3n-sdk": "5.2.0",
    "tsx": "^4.20.6"
  }
}
